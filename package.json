{
  "name": "church-media-server",
  "version": "1.0.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "build:watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "hash-password": "npm run build && node dist/scripts/hash-password.js",
    "start": "npm run build && node dist/main.js",
    "test": "npm run build && node --test --test-concurrency=1 dist/tests/ut/*.test.js",
    "test:watch": "node --test --watch --test-concurrency=1 dist/tests/ut/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "ffi-napi": "^4.0.3",
    "osc": "^2.4.5",
    "ref-array-napi": "^1.2.2",
    "socket.io": "^4.7.2",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
