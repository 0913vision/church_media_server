node_modules/
.env
*.mp3
*claude*
.DS_Store
dist/
data/
__pycache__/

# The track manifest is the server's own file: it rewrites it when a level
# changes. assets/tracks.example.json is the copy to start from.
assets/tracks.json
